The CLI interface has been redesigned. Instead of specifying `--classpath` and the `prefixes` to generate stubs for, only the input file(s) need to be provided, and stubs are generated for all classes contained within them.
