Multiple inputs can now be passed at once. In addition to `.jar` files, `.aar` archives, Android API level shorthands (e.g. `android-35`), and Maven coordinates (e.g. `androidx.appcompat:appcompat:1.0.2`) are now supported as inputs and are downloaded and cached automatically.
