Stub generation now uses the ASM bytecode library instead of Java Reflection. This allows stubs to be generated for *all* classes in a `.jar`, including Android-compiled classes that cannot be loaded into a standard JDK runtime.